import { expect } from 'chai';
import { shallowMount } from '@vue/test-utils';
import Init from '@/popup/pages/Init.vue';

describe('Init page test', () => {
  test('render init pages', () => {
    console.log('test');
  });
});
